type Params = {
    transferId: number;
};

// GET /transfer/:id/detail
// summary: 振込申請詳細表示
// page: /transfer/detail/[id]
export const getTransferDetailUseCase = async ({ transferId }: Params) => {
    // データ取得
    
};
