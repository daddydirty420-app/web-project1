export type PointLots = {
    id: string;
    points: number;
    used_points: number;
    expires_at: Date;
    alertPoints: number;
};

export type UriagekinLots = {
    id: string;
    uriagekin: number;
    used_Uriagekin: number;
    expires_at: Date;
    alertUriagekin: number;
};
